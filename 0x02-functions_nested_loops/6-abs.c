#include <stdio.h>
#include "main.h"
#include <ctype.h>
#include <stdlib.h>

/**
 * _abs - returns only possitive values
 * @c: is an integer value
 * Return: 0 always
 */
int _abs(int c)
{
	int i;

	i = abs(c);
	return (i);
}
